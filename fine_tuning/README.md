Code for fine-tuning models on RaLEs datasets.

Inputs:
- pre-trained model path on HuggingFace model zoo
- path to prepared RaLEs dataset(s)

Outputs:
- A trained, fine-tuned model for each dataset of interest.